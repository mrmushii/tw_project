# Local dependencies only. VGG-16 training runs on Kaggle (GPU) — TensorFlow is NOT installed locally.
anthropic>=0.40.0
python-dotenv>=1.0.0
pillow>=10.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
kagglehub>=0.3.0
tqdm>=4.66.0
